# Mystic Tarot Backend Dependencies
# Python 3.11+

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# AI & ML
replicate==1.0.4
openai==1.58.1

# Firebase
firebase-admin==6.6.0

# HTTP Client
httpx==0.28.1

# Environment & Config
python-dotenv==1.0.1

# Data Validation (included with FastAPI, but pinned for clarity)
pydantic==2.10.4

# Astrology Calculations (Swiss Ephemeris wrapper)
kerykeion==4.14.0

# ElevenLabs TTS (uses httpx for async streaming)
# httpx is already included above

# Security
python-jose[cryptography]==3.3.0

# ASGI Server (production)
gunicorn==23.0.0
